
public class ReferenceDataType {

	public static void main(String[] args) {
		// 기본형 타입
		int i = 4; // 4byte 크기의 정수형 변수 i에 4를 대입
		
		// 참조형 타입
		String str = new String("Hello");
		  // new: 클래스를 메모리에 올리라는 명령어
		  // String 생성자: 메모리에 객체(인스턴스)를 만들라는 명령어
		  // 객체(인스턴스): 메모리에 올라간 클래스
		  // str: String 객체(인스턴스)를 참조하는 변수. 메모리의 주소가 저장된다.
	}

}
